---

# 小智 ESP32 的 MCP 平台 - 系统设计介绍（开源版）

> 本文档基于当前仓库实现整理，用于对齐“代码现状”与“对外介绍”。

## 1. 项目定位

本项目是一个面向小智设备生态的 MCP（Model Context Protocol）服务管理平台。

核心目标：

- 为用户提供可直接接入的 MCP 服务目录
- 支持用户绑定小智控制台生成的 WSS 地址并管理实例生命周期
- 提供注册/登录、邀请码、激活码、有效期等基础运营能力

## 2. 当前技术栈（与代码一致）

- 前端：React + TypeScript + Vite
- 后端：Node.js + TypeScript + Express + TypeORM
- 主数据库：MySQL 8+
- 可选能力：报告处理 Worker（依赖 PostgreSQL + 向量相关能力）

## 3. 开源版能力边界（重要）

当前开源版已明确下线并移除：

- 知识库上传/检索相关前后端能力
- 知识库路由、控制器、工具注册、Worker 入口

因此本仓库对外应统一表述为：

- ✅ MCP 管理核心能力完整可用
- ❌ 知识库能力不在开源版范围内

## 4. 核心模块现状

### 4.1 用户与认证

- 邮箱注册/登录
- 邮箱验证码（登录、注册、找回密码）
- 邀请码体系（邀请人与被邀请人有效期延长）
- 账号有效期控制
- 管理员用户管理能力

### 4.2 MCP 服务市场

- 服务目录由 `mcp_service` 表提供
- 用户可浏览服务并创建实例
- 开源版已过滤知识库相关服务展示

### 4.3 MCP 实例管理

- 用户绑定 WSS 地址创建实例
- 支持启动/停止/删除
- 后端在服务启动时会根据服务类型注册对应工具
- 支持服务重启后的实例自动恢复（按状态拉起）

## 5. 数据模型（按当前实体）

当前后端实体主要包括：

- `User`
- `MCPService`
- `UserMCPInstance`
- `ActivationCode`
- `VerificationCode`

说明：TypeORM 使用默认命名策略，实际表名以当前数据库为准；核心依赖表为 `mcp_service`。

## 6. 初始化与部署流程（当前推荐）

### 6.1 本地初始化

1. 配置 `backend/.env`（MySQL 连接等）
2. 执行数据库初始化：
   - `npm run db:init`
   - `npm run db:seed`
   - 或一键：`npm run db:bootstrap`

### 6.2 关键事实

- 新用户开箱可用最小条件：`mcp_service` 有基础数据
- 当前仓库已提供 `backend/mcp_service.sql` 与 `db:seed` 双路径

### 6.3 生产部署

请以根目录 `DEPLOY.md` 为准（已同步到当前开源版本流程）。

## 7. MCP 协议与接入说明

- 平台通过 WebSocket 与小智侧接入点进行 MCP 通信
- 用户在小智控制台获取 WSS 地址后填写到实例配置
- 后端根据实例对应服务提供 tool list 与 tool call 能力

## 8. 对外入口

- 官网体验：<https://facaiai.cn>
- 开源仓库：<https://github.com/chenlikun2010/xiaozhi-esp32-mcp>

## 9. 文档维护约定

后续如出现以下变更，应同步更新本文档：

- 服务上下线（尤其是功能边界变化）
- 初始化脚本与部署流程变化
- 鉴权策略、数据模型、核心路由变更

---
