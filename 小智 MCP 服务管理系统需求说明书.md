
---

# 小智 MCP 接入云平台 - 系统需求说明书 (SRS)

## 1. 项目概述
### 1.1 项目背景
目前用户在使用“小智 AI (xiaozhi.me)”的 MCP 功能时，往往需要具备一定的技术背景来部署和维护 MCP 服务端。本项目旨在降低用户门槛，提供一个预集成多种 MCP 服务的云平台。

### 1.2 核心目标
用户只需在平台上选择所需服务，并粘贴小智官网提供的 MCP 接入点地址，即可实现一键连接与管理，无需自行部署环境。

---

## 2. 用户角色
*   **普通用户**：通过邮箱注册，使用 MCP 服务，管理自己的接入点，参与邀请奖励。
*   **管理员**（后续扩展）：管理 MCP 服务列表，监控系统运行状态，处理异常。

---

## 3. 功能需求

### 3.1 账户与认证系统
*   **邮箱注册/登录**：仅支持邮箱注册。
*   **身份验证**：采用 JWT 或 Session 机制保持登录状态。
*   **邀请码系统**：
    *   每个用户注册后自动生成唯一邀请码。
    *   新用户注册时可填写邀请码。
    *   **激励逻辑**：邀请成功后，邀请人与被邀请人的服务使用期限同步延长 7 天。

### 3.2 MCP 服务中心 (核心)
*   **服务列表展示**：展示平台提供的所有 MCP 服务（如：TNL 联网搜索、文件读取、数据库操作等）。
*   **服务详情**：每个服务包含名称、描述、功能介绍。
*   **服务选择**：用户可选择一个或多个 MCP 服务进行激活。

### 3.3 MCP 接入管理
*   **接入配置**：用户在选定服务后，需输入“小智 MCP 接入点地址”。
    *   *示例格式*：`wss://api.xiaozhi.me/mcp/?token=...`
*   **连接控制**：
    *   **启动连接**：点击启动后，平台后端作为 MCP Server 尝试连接用户提供的 WSS 地址。
    *   **停止连接**：点击停止后，断开当前 WebSocket 连接并释放资源。
*   **多服务管理**：用户可以在控制面板看到已开启的所有 MCP 服务实例，并分别进行启停操作。

### 3.4 状态监控
*   **连接状态实时显示**：
    *   `未连接`：未配置或已停止。
    *   `连接中`：正在进行握手。
    *   `已连接`：连接成功，服务可用。
    *   `异常`：连接断开或 Token 失效。
*   **运行时长统计**：记录并显示服务自启动以来的运行时间。

---

## 4. 界面设计 (UI/UX)
参考 `mcp.shanmaotech.cn` 的布局：
*   **侧边栏/顶部导航**：首页/仪表盘、MCP 服务市场、邀请奖励、个人中心。
*   **仪表盘 (Dashboard)**：
    *   顶部展示个人邀请码及剩余有效期。
    *   中部展示“已选服务”列表。
    *   列表列名：服务地址（脱敏显示）、MCP 模型/服务类型、当前状态、启动时间、操作（停止/启动/删除）。
*   **弹窗交互**：点击“获取 MCP 接入点”或“新增服务”时，弹出输入框引导用户粘贴小智官网的 WSS 地址。

---

## 5. 非功能需求
*   **并发性**：后端需支持多用户同时维持长连接（WebSocket）。
*   **稳定性**：若连接异常断开，需具备自动尝试重连机制（可选）或向用户发送通知。
*   **安全性**：
    *   用户填写的 Token 需加密存储或仅在内存中运行。
    *   防止 WebSocket 注入攻击。

---

## 6. 技术栈建议 (供 AI 编程参考)
*   **前端**：React.js (搭配 Tailwind CSS + Ant Design 或 Shadcn UI)。
*   **后端**：Node.js (NestJS) 或 Python (FastAPI/Go)，适合处理高并发 WebSocket。
*   **数据库**：MySQL (存储用户信息、服务状态、邀请记录)；Redis (处理连接状态缓存)。数据库用：数据库：8.140.51.220，数据库：mcplist，账号：mcpadmin 密码：Mcp@20260109，端口3306

*   **MCP 协议实现**：集成 `@modelcontextprotocol/sdk`。必须参考 https://github.com/78/mcp-calculator。核心逻辑：本平台作为 MCP Server，通过 WebSocket (WSS) 反向连接到小智提供的接入点 URL，建立 JSON-RPC 通信隧道。

---

## 7. 数据模型简化 (Schema)
*   **User**: `id, email, password, invitation_code, referred_by, expire_date`
*   **MCP_Service**: `id, name, description, image_url`
*   **User_MCP_Instance**: `id, user_id, service_id, xiaozhi_wss_url, status, start_time`

---

## 8. 业务流程图 (逻辑参考)
1.  **用户**：小智官网 -> 复制 MCP 接入点。
2.  **本平台**：登录 -> 选择“联网搜索”服务 -> 粘贴地址 -> 点击“启动”。
3.  **后端**：验证有效性 -> 初始化 MCP 客户端 -> 拨号连接 `wss://api.xiaozhi.me/...` -> 建立双向通信隧道。
4.  **结果**：小智官网显示“已连接”，用户可在对话框中使用该 MCP 能力。

